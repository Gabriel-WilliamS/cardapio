export interface ICreateMenu {
  userId: string;
  name: string;
}

export interface IUpdateMenu {
  id: string;
  userId: string;
  name: string;
}

export interface IDeleteMenu {
  id: string;
  userId: string;
}
